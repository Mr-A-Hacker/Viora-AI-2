# Web & API
python-dotenv>=1.0.1
fastapi>=0.110.0
apscheduler>=3.10.0
uvicorn>=0.29.0
pydantic>=2.7.0
websockets>=12.0
flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0
requests>=2.31.0

# Camera & video (OpenCV used by backend and camera stream)
opencv-python>=4.9.0

# System & async
psutil>=5.9.0
numpy>=1.26.0

# Audio I/O (STT capture)
# System dependency (Debian/Ubuntu): sudo apt-get update && sudo apt-get install -y portaudio19-dev
PyAudio>=0.2.14

# Speech-to-text
faster-whisper>=1.0.0
vosk>=0.3.45
scipy>=1.11.0

# Text-to-speech (Piper)
piper-tts>=1.2.0
onnxruntime>=1.17.0

# Local LLM & model download
llama-cpp-python>=0.2.0
huggingface-hub>=0.20.0

# Web search (tool_ai)
ddgs>=9.0.0
beautifulsoup4>=4.12.0

# Semantic routing (semantic_router_ai.py)
semantic-router[local]>=0.0.70

# Optional: Raspberry Pi camera (install on device; often via apt or --system-site-packages)
# picamera2==0.3.34

# Hailo object detection (object_detection.py + hailo_od)
lap>=0.4.0
cython_bbox  # may need building on Pi; install from project .venv
PyYAML>=6.0

# Testing (optional): pip install pytest then run pytest tests/
pytest>=8.0
